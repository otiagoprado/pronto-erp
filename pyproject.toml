[project]
name = "pronto_erp"
version = "0.0.1"
description = "Tema customizado e otimizacoes para o ERP Pronto"
dependencies = [
    "frappe"
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
